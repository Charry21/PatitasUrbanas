services:
  postgres_db:
    image: postgres:16
    container_name: patitas_urbanas_db
    command: >
      postgres -c shared_preload_libraries=pg_stat_statements
               -c pg_stat_statements.track=all
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: patitas_urbanas
    ports:
      - "5433:5432"
    volumes:
      - pgdata_v16:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d patitas_urbanas"]
      interval: 10s
      timeout: 5s
      retries: 5

  api_app:
    build: ./app
    container_name: patitas_urbanas_api
    ports:
      - "3000:3000"
    depends_on:
      postgres_db:
        condition: service_healthy
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=adminpassword
      - POSTGRES_DB=patitas_urbanas
      - DB_HOST=postgres_db
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_db:5432/patitas_urbanas

volumes:
  pgdata_v16:
